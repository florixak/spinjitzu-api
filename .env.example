# App
NODE_ENV=development
PORT=3000

# Database (Neon)
DATABASE_URL=          # pooled connection – runtime
DIRECT_DATABASE_URL=   # direct connection – migration

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=86400
ADMIN_EMAIL=admin@email.com
ADMIN_PASSWORD=admin_password
